from discord import app_commands
import httpx
import logging

# def register_joinserver(tree, guild, url, token):
#     @tree.command(
#         name='joinserver',
#         description="Turns off security of the server, join, then the security comesbackup.",
#         guild=guild
#     )
#     async def joinserver(interaction: Interaction)
#         user = interaction.user
